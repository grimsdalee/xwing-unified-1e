# TTS Explorer Patch

Copy the contents of this ZIP into your repository root.

Then run:

```cmd
cd C:\Users\Evan\Documents\GitHub\xwing-unified-1e\tools\TtsExplorer
dotnet build
dotnet run
```

Open:

```text
source\unified-2.5\2486128992.json
```

Once confirmed working:

```cmd
cd C:\Users\Evan\Documents\GitHub\xwing-unified-1e
git add .
git commit -m "Add TTS Explorer developer tool"
git push
```
